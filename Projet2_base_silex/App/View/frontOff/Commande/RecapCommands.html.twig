{% extends "layout.html.twig" %}
{% block contenu %}
    {% if   app.session.get('logged') is defined and  app.session.get('logged') == 1 %}
        <div class="row">
            <div class="col-md-6 col-lg-offset-3">
                <table class="table table-responsive table-bordered">
                    <caption><h4>Mes Commandes</h4></caption>
                    <thead>
                    <tr class="table text-capitalize"><th>Numéro de Commande</th><th>Date d'achat</th><th>Status</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for commande in data if data is not empty %}
                    <tr class="">
                        <td>{{ commande.id }}</td><td>{{ commande.date_achat }}</td><td class="text-warning">{{ commande.libelle }}</td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    {% endif %}
{% endblock %}